# 01 — Suggestion Prepension & Pinned State for SDK Commands

**What to build:**
When a developer enters `sdk tstruct`, `sdk iview`, `sdk page`, or `sdk axpert data sources` (including `sdk ads`), the "Create New" suggestion option is prepended as the top item in Axi suggestions and remains pinned at the top regardless of partial search input.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Prepend "Create New" suggestion item for `sdk tstruct`, `sdk iview`, `sdk page`, and `sdk ads`.
- [ ] Pin "Create New" as the first suggestion item during partial search filtering.
- [ ] Render action badge icon for "Create New" suggestion item.
